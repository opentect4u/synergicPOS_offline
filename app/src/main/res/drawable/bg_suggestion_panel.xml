<?xml version="1.0" encoding="utf-8"?>
<!--
  The sheet the search suggestions sit on.

  White with a hairline edge and the same 12dp corners the search box above it uses,
  so the panel reads as the box having opened rather than as a separate window that
  happened to appear over the grid. ListPopupWindow draws its own elevation.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#E4E7EB" />
</shape>
